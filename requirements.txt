# Core
openai>=1.30.0
python-telegram-bot>=21.0

# Utilities
python-dotenv>=1.0.0
httpx>=0.27.0
tiktoken>=0.7.0
ddgs>=8.0.0

# Admin Portal (web UI for setup & config)
fastapi>=0.100.0
uvicorn>=0.20.0

# API key encryption at rest
cryptography>=42.0.0

# Optional: Anthropic provider
# anthropic>=0.25.0

# Optional: Native vector search (faster memory recall when embedding is enabled)
# sqlite-vec>=0.1.6

# Optional: WeChat transport (iLink API)
# aiohttp>=3.9
